VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Dipakai oleh runner migrasi (scripts/migrate.mjs).
# WAJIB connection string Postgres Supabase mode SESSION / "Direct connection":
#   host: db.<project-ref>.supabase.co   port: 5432
# JANGAN pakai transaction pooler (port 6543) — DDL seperti ALTER TABLE tidak
# boleh lewat pooler, harus lewat koneksi langsung.
# Format: postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres
DATABASE_URL=
